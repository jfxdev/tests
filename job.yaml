title: Hello World
description: Example with a Hello World message
image: docker
cron: "* * * * *"
command:
  - docker
  - ps
tags:
  - local
mode: once_all
volumes:
  - /var/run/docker.sock:/var/run/docker.sock
