name: Hello World
description: Example with a Hello World message
image: docker
cron: "* * * * *"
resources:
  cpu: 1
  memory: 1gb
  timeout: 30s
command:
  - docker
  - ps
tags:
  - local
  - node
mode: once_all
volumes:
  - /var/run/docker.sock:/var/run/docker.sock
